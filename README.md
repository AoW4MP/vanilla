# Vanilla (AOW4 Database mirror)

Fork of the [Age of Wonders 4 Database](https://minionsart.github.io/aow4db/) with:

- **Base path** `/vanilla/` for GitHub Pages (or your host) instead of `/aow4db/`
- **Draft Lobby** — multiplayer draft tool (from ABM), under Builds/Planners

Deploy the repo so the site is served at e.g. `https://your-org.github.io/vanilla/`.
